<%= form_with model: @cate, local: true do |form| %>
<div class="form-group">
          <% if @cate.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@cate.errors.count, "error") %> prohibited
              this article from being saved:</h2>
            <ul>
            <% @cate.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
          <% end %>

  <p>
    <%= form.label :name %>
    <%= form.text_field :name ,class: "form-control" %>
  </p>

  <p>
    <%= form.label :category_id %>
    <%= form.collection_select :id, Category.all, :id, :name, include_blank: true, class: "custom-select custom-select-lg mb-3" %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
<% end %>